# === Suite: topology-authority ===
# pushing a mutated map to topo-auth-nodes-0 at stale version 4294967302 (its current version is 4294967303)
# pass held at the pre-send point with version 4294967306
# pass held at the pre-send point with version 4294967313; taking the lease away now
# pass held at the pre-send point with version 4294967315; deleting the lease so the read fails
1..4
ok 1 - flared rejects a topology push carrying a stale version
ok 2 - control: holding the lease, the stopped pass does broadcast
ok 3 - lease taken by another identity: the stopped pass reaches the fence and does not send
ok 4 - lease unreadable: the stopped pass fails closed and does not send

--- what each test required ---
2/3/4 stop the SAME production pass at one point: committed, version advanced,
pre-send lease check not yet made (FLARE_TEST_PRESEND_BARRIER seam in Main.lean).
Reaching that point is proved by the operator writing the held version to
<barrier>/reached; the tests then assert the fence/broadcast branch was taken
for THAT version. A receiver standing still was not accepted as evidence.

--- environment ---
local kind v0.31.0, Docker Desktop, arm64 darwin
images built from the working tree; base operator memory limit raised to 1Gi by hand
(the per-suite harness ceiling is raised in this change and applies on every arch)
command: cd flare_operator && .lake/build/bin/flare_e2e --filter topology-authority
